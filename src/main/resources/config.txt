DATA_FILE=/Users/dmitrijmartynov/Desktop


реализовать паттерн Builder для создания объекта

вынести получение следующего за вызываемым аргумента в отдельный метод
 (update {id}) <- вынести реализацию чтения id с клавиатуры с помощью <T> объекта

правильно отлавливать ошибки чтобы при запуске jar корректно создавался файл для сохранения

сделать чтобы программа работала, пока не будет введено слово exit (оч сложно звучит по моему)
+отловить ошибки пустой коллекции (не выводить info если коллекция пустая)
сделаь команду info более информативной (то есть выводить название и id элементов коллекции)

почистить ненужные файлы (exception)

пофиксить баг с insert (insert 2 -> remove 2 -> insert ) баг идет

написать парсер свой с помощью регулярных выражений для парсера

сделать сериализацию/десириализацию объекта для хранения с помощью класса ObjectInput/OutputStream
для резервного копирования данных

удалить из ColletionController методы синхронизации данных корретно(ведь перезаписывается одна и та же ссылка)

команда exit - переделать диалог с пользователем на: хотите сохранить измениния y/n вместо хотите
выйти без сохранения

влить ветку replace_if_lowe

поработать над красотой вывода команды GroupCountingByIdCommand

[-330, 414, 397, -38; 414, -567, -414, -119; 397, -414, -626, 340; -38, -119, 340, -588;]

разобраться с javadoc

ошибка permission denied при запросе на любой порт - решить!